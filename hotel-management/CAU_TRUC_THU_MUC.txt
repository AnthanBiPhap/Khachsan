hotel-management
│
├── .next                     // Build output của Next.js (tự động generate)
│
├── app                       // Next.js App Router (pages/routes)
│
├── components                // Component tái sử dụng (UI, header, footer...)
│
├── contexts                  // React Context (AuthContext...)
│
├── hooks                     // Custom hooks (useBookingNotifications, useRoomSearch...)
│
├── lib                       // Thư viện helper (stripe, utils)
│
├── node_modules             // Đống thư viện cài bằng npm/pnpm/yarn
│
├── public                    // File tĩnh (images, logo, placeholder...)
│
├── services                  // API service layer (gọi API backend)
│
├── styles                    // CSS files
│
├── utils                     // Utility functions (soundNotification...)
│
├── .env.local                // Biến môi trường local
├── .gitignore                // File ignore cho Git
├── components.json           // Cấu hình shadcn/ui
├── next-env.d.ts             // Next.js type definitions
├── next.config.mjs           // Cấu hình Next.js
├── package-lock.json         // Lock version khi dùng npm
├── package.json              // Thông tin dự án + dependencies + scripts
├── pnpm-lock.yaml            // Lockfile của pnpm
├── postcss.config.mjs        // Cấu hình PostCSS
├── tsconfig.json             // Cấu hình TypeScript
└── yarn.lock                 // Lockfile của yarn

