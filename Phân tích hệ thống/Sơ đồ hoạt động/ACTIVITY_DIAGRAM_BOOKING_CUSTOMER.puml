@startuml Activity_Booking_Customer
!theme plain

|Customer|
start
:Quản lý booking;

|System|
if (Đặt phòng?) then (có)
    :Kiểm tra tính khả dụng phòng;
    if (Phòng khả dụng?) then (không)
        :Phòng không khả dụng;
    else (có)
        :Đặt phòng thành công;
    endif
else (không)
    :Kiểm tra điều kiện và thực hiện CRUD;
    if (Thành công?) then (không)
        :Hiển thị lỗi;
    else (có)
        :Thực hiện thành công;
    endif
endif

|Customer|
:Hiển thị kết quả;
stop

@enduml

