@startuml Activity_ImportExport_Customer
!theme plain

|Customer|
start
:Quản lý import/export;

|System|
if (Import file Excel?) then (có)
    :Đọc và validate file Excel;
    if (Dữ liệu hợp lệ?) then (không)
        :Dữ liệu không hợp lệ;
    else (có)
        :Import thành công;
    endif
else (không)
    :Kiểm tra điều kiện và thực hiện CRUD;
    if (Thành công?) then (không)
        :Hiển thị lỗi;
    else (có)
        :Thực hiện thành công;
    endif
endif

|Customer|
:Hiển thị kết quả;
stop

@enduml

