@startuml Activity_Dang_Nhap
!theme plain

|User|
start
:Điền form đăng nhập;

|System|
:Tìm user và kiểm tra điều kiện đăng nhập;
if (Điều kiện hợp lệ?) then (không)
    :Hiển thị lỗi;
else (có)
    :So sánh mật khẩu với bcrypt;
    if (Mật khẩu đúng?) then (có)
        :Đăng nhập thành công;
    else (không)
        :Email hoặc mật khẩu sai;
    endif
endif

|User|
:Hiển thị kết quả;
stop

@enduml

