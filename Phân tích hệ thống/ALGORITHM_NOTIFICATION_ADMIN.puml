@startuml Algorithm - Quản lý thông báo
!theme plain
title Sơ đồ thuật toán - Quản lý thông báo

start
if (Quản lý template?) then
 :Input: templateId, content, variables;
 :Kiểm tra quyền Admin;
 :Cập nhật/Create template;
 :Lưu vào database;
elseif (Cấu hình TTL?) then
 :Input: notificationType, TTL (days);
 :Cập nhật TTL;
elseif (Cấu hình retry?) then
 :Input: notificationType, retryPolicy;
 :Cập nhật retry policy;
elseif (Bật/tắt loại?) then
 :Input: notificationType, enabled (true/false);
 :Cập nhật enabled status;
endif
:Return SUCCESS;
stop
@enduml

