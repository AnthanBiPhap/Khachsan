@startuml Algorithm - Quản lý thông tin cá nhân
!theme plain
title Sơ đồ thuật toán - Quản lý thông tin cá nhân

start
:Input: userId, updatedData;
:Validate userId;
:Validate dữ liệu;
if (Email thay đổi?) then (Có)
 :Kiểm tra email hợp lệ;
 :Kiểm tra email trùng?;
 if (Email trùng?) then (Có)
 :Return ERROR;
 stop
 endif
endif
if (Có avatar?) then (Có)
 :Upload và xử lý avatar;
endif
:Cập nhật thông tin;
if (Email thay đổi?) then (Có)
 :Set emailVerified = false;
 :Gửi email xác thực lại;
endif
:Return SUCCESS;
stop
@enduml

