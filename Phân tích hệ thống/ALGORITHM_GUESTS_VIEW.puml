@startuml Algorithm - Xem danh sách khách
!theme plain
title Sơ đồ thuật toán - Xem danh sách khách

start
:Input: userId, role, filters (name, email, phone);
:Kiểm tra quyền Staff/Admin;
:Lấy danh sách guests;
if (Có filter tên?) then (Có)
 :Tìm kiếm theo tên (LIKE);
endif
if (Có filter email?) then (Có)
 :Tìm kiếm theo email (LIKE);
endif
if (Có filter phone?) then (Có)
 :Tìm kiếm theo số điện thoại (LIKE);
endif
:Sắp xếp theo createdAt DESC;
:Phân trang;
:Return danh sách guests;
stop
@enduml

