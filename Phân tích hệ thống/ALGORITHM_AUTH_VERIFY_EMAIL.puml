@startuml Algorithm - Xác thực email
!theme plain
title Sơ đồ thuật toán - Xác thực email

start
:Input: verification token;
:Validate token;
:Tìm token trong database;
if (Token không tồn tại?) then (Có)
 :Return ERROR;
 stop
endif
:Kiểm tra token hết hạn?;
if (Token hết hạn?) then (Có)
 :Xóa token;
 :Return ERROR;
 stop
endif
:Lấy user từ token;
if (Email đã xác thực?) then (Có)
 :Đánh dấu token đã dùng;
 :Return SUCCESS;
 stop
endif
:Cập nhật emailVerified = true;
:Đánh dấu token đã dùng;
:Xóa token cũ;
:Return SUCCESS;
stop
@enduml

